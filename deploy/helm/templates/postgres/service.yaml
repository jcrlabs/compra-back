apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  labels:
    {{- include "compra-back.labels" . | nindent 4 }}
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP

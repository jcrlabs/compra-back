apiVersion: v1
kind: Service
metadata:
  name: {{ include "compra-back.fullname" . }}
  labels:
    {{- include "compra-back.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
  selector:
    {{- include "compra-back.selectorLabels" . | nindent 4 }}

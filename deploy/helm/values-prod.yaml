replicaCount: 2

image:
  tag: v0.1.17

ingress:
  host: compra-api.jcrlabs.net

config:
  corsAllowedOrigins: "https://compra.jcrlabs.net"

postgres:
  storage: 20Gi

hpa:
  minReplicas: 2
  maxReplicas: 5
